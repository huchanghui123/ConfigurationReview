<Window x:Class="QotomReview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QotomReview"
        mc:Ignorable="d"
        Title="Ootom Review V1.00" Height="800" Width="850" Closing="OnClosing">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock }" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontFamily" Value="Comic Sans MS"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="22"/>
        </Style> 
        <Style TargetType="{x:Type TextBox }">
            <Setter Property="FontFamily" Value="Comic Sans MS"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="{x:Type Button }">
            <Setter Property="FontFamily" Value="Comic Sans MS"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="FontFamily" Value="Comic Sans MS"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Check}" Value="error">
                    <Setter Property="Foreground" Value="Red" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Canvas Grid.Column="0" Grid.ColumnSpan="1">
            <StackPanel Canvas.Left="10" Canvas.Top="5" >
                <TextBlock Text="Windows edition" />
                <TextBlock Text="System type" />
                <TextBlock Text="System language" />
                <TextBlock Text="Processor" />
                <TextBlock Text="Motherboard" />
                <TextBlock Text="BIOS type" />
                <TextBlock Text="CPU temperature" />
                <TextBlock Text="Memory info" Margin="0,130,0,0" />
                <TextBlock Text="Storage info" Margin="0,80,0,0" />
                <TextBlock Text="Network info" Margin="0,80,0,0" />
                <TextBlock Text="NTP synchronize" Margin="0,130,0,0" />
                <TextBlock Text="Computer name" Margin="0,10,0,0"/>
            </StackPanel>
        </Canvas>
        <Canvas Grid.Column="1" Grid.ColumnSpan="5">
            <StackPanel Canvas.Left="0" Canvas.Top="5" Width="692" >
                <TextBlock x:Name="os" Text="Loading..." />
                <TextBlock x:Name="os_type" Text="Loading..."/>
                <TextBlock x:Name="os_language" Text="Loading..."/>
                <TextBlock x:Name="cpu" Text="Loading..." />
                <TextBlock x:Name="mb" Text="Loading..." />
                <TextBlock x:Name="bios" Text="Loading..." />

                <ListView x:Name="temperatureList" Height="150" BorderThickness="1,1,1,1" BorderBrush="#FFC8CCD4" >
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding Path=Name}" />
                            <GridViewColumn Header="Value" Width="120" DisplayMemberBinding="{Binding Path=Value}" />
                            <GridViewColumn Header="MinValue" Width="120" DisplayMemberBinding="{Binding Path=MinValue}" />
                            <GridViewColumn Header="MaxValue" Width="120" DisplayMemberBinding="{Binding Path=MaxValue}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <ListView x:Name="mem_List" Height="100" BorderThickness="1,1,1,1" BorderBrush="#FFC8CCD4" >
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Path=Name}" />
                            <GridViewColumn Header="Speed" Width="120" DisplayMemberBinding="{Binding Path=Speed}" />
                            <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding Path=Type}" />
                            <GridViewColumn Header="Size" Width="120" DisplayMemberBinding="{Binding Path=Size}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <ListView x:Name="disk_List" Height="100" BorderThickness="1,1,1,1" BorderBrush="#FFC8CCD4" >
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="300" DisplayMemberBinding="{Binding Path=Name}" />
                            <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding Path=Type}" />
                            <GridViewColumn Header="Size" Width="120" DisplayMemberBinding="{Binding Path=Size}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <ListView x:Name="net_List" Height="150" BorderThickness="1,1,1,1" BorderBrush="#FFC8CCD4">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Adapter Description" Width="260" DisplayMemberBinding="{Binding Path=Adapter}" />
                            <GridViewColumn Header="MAC Address" Width="150" DisplayMemberBinding="{Binding Path=Mac}" />
                            <GridViewColumn Header="IP Address" Width="120" DisplayMemberBinding="{Binding Path=Ip}" />
                            <GridViewColumn Header="Status" Width="60" DisplayMemberBinding="{Binding Path=Status}" />
                            <GridViewColumn Header="Type" Width="80" DisplayMemberBinding="{Binding Path=Type}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="NTP server：" />
                    <TextBox x:Name="ntp_server" Text="time.windows.com" Width="150"/>
                    <Button Content="Update time" Click="UpdateTimeClick" Margin="20,0,0,0"/>
                    <TextBlock x:Name="time" Text="System time" Margin="20,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Computer name：" />
                    <TextBox x:Name="input_name" Text="Loading..." Width="150" />
                    <Button Content="Generate name" Click="GenerateNameClick" Margin="20,0,0,0"/>
                    <Button Content="Update name" Click="UpdateNameClick" Margin="20,0,0,0"/>
                    <TextBlock Text=" (need to restart)" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Save configuration" Click="SaveConfigClick" />
                    <CheckBox x:Name="check" IsChecked="True" Content="对比上一次配置" Margin="10,0,0,0" VerticalContentAlignment="Center" />
                    <TextBlock x:Name="save_config" Text="加载配置..." Margin="20,0,0,0"/>
                </StackPanel>

            </StackPanel>
        </Canvas>

    </Grid>
</Window>
